
export default function Modal ({close}){
    return(
        <div onClick={close} className={'fixed inset-0 flex justify-center items-center transition-colors'}>
          hhhhh
        </div>
    )
}